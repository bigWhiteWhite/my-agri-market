import {ADD_TODO,DELETE_TODO,RECEIVE_TODOS,ADD_NUM} from './mutation-type'
export default {
    /* addTodo({commit},todo){
        commit(ADD_TODO,{todo})
    },
    deleteTodo({commit},index){
        commit(DELETE_TODO,{index})
    },
    reqTodos({commit}){
        setTimeout(()=>{
            const todos=storageUtil.readToos()
            commit(RECEIVE_TODOS,todos)
        },1000)
    }, */
	
}